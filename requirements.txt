# Regional Language Study Bot - Requirements
# Translation is now handled by Azure AI Translator (REST API via `requests`),
# so the heavy local NLLB stack (torch, transformers, accelerate,
# sentencepiece, sacremoses, protobuf) has been removed entirely.

# SQLite shim -- some deployment platforms ship an sqlite3 older than the
# >=3.35 that Chroma requires.
pysqlite3-binary; sys_platform == "linux"

# Streamlit for web interface
streamlit>=1.60,<2

# Groq LLM integration
langchain-groq>=1.1,<2
langchain-core>=1.5,<2
langchain-community>=0.4,<0.5
langchain-text-splitters>=1.0,<2

# Google Gemini Embeddings
google-genai==2.14.0

# Document processing
pypdf>=6.14,<7
python-docx>=1.2,<2
docx2txt>=0.8,<1

# Vector storage - ChromaDB
chromadb>=1.5,<2

# Basic utilities
requests>=2.32,<3
pydantic>=2.13,<3
python-dotenv>=1.1,<2
typing-extensions>=4.12,<5
